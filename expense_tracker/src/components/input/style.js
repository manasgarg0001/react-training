import { makeStyles } from "@mui/styles";

export const Inputstyle = makeStyles({
  styleInput: {
    height: "30px",
    width: "100%",
    borderRadius: "20px",
    boxShadow: " 2px 2px grey",
  },
});
